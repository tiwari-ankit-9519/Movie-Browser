# The website is created using the TMDB movie API

- Tailwind CSS for the styling part
- Used custom hooks to manage the states
- Used React Router for client-side routing
- Used Axios for making API requests
- Used React Query for caching and managing data fetching
- Used React Icons for icons
